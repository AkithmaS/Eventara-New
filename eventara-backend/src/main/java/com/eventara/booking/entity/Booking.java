package com.eventara.booking.entity;

// @Entity extends BaseEntity
// id (UUID), user (ManyToOne), event (ManyToOne),
// bookingReference (unique, generated), totalAmount, status (BookingStatus),
// paymentStatus, paymentReference, expiresAt (for lock TTL),
// @OneToMany bookingItems (seats)
