package com.eventara.event.entity;

// @Entity extends BaseEntity
// id (UUID), title, description, bannerUrl, organizer (ManyToOne),
// category (ManyToOne), venue, city, startDateTime, endDateTime,
// totalCapacity, status (EventStatus enum), isPublic,
// @OneToMany seatSections, @OneToMany pricingTiers
