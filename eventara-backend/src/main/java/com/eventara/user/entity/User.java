package com.eventara.user.entity;

// @Entity extends BaseEntity
// id (UUID), fullName, email (unique), passwordHash, phone,
// avatarUrl, role (enum), status (ACTIVE/BANNED/PENDING_APPROVAL),
// emailVerified, createdAt, updatedAt
// @OneToMany bookings, @OneToOne organizerProfile
