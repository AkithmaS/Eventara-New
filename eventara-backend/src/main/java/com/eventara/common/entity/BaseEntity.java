package com.eventara.common.entity;

// @MappedSuperclass
// id (UUID, @GeneratedValue strategy UUIDs),
// createdAt (@CreatedDate), updatedAt (@LastModifiedDate)
// Uses @EntityListeners(AuditingEntityListener.class)
