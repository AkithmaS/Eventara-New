package com.eventara.admin.repository;

// Placeholder removed — use AuditLogRepository for admin-specific queries.
// All entity repositories (User, Organizer, Event, etc.) are injected
// directly into AdminServiceImpl from their respective packages.
