package com.eventara.common.config;

// JWT properties are bound directly in JwtUtil via @Value.
// This file is intentionally left as a placeholder for future
// JWT-related configuration beans (e.g. token blacklist, refresh config).
